import React from "react";
import styles from "./DesignButton.module.scss";

const DesignButton = () => {
  return <button className={styles.button}>დიზაინი</button>;
};

export default DesignButton;
