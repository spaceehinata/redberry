// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "momentum.redberryinternship.ge",
      "via.placeholder.com",
      "api.dicebear.com", 
      "cdn.builder.io", 
    ],
  },
};

module.exports = nextConfig;
